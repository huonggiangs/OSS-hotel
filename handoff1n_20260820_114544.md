# Handoff 1n — 2026-08-20 11:45:44 (+07:00)

## Phạm vi phiên

- Rà soát dự án ở chế độ chỉ đọc, không thay đổi mã nguồn hay cấu hình ứng dụng.
- Xác minh TypeScript cho `webadmin` API/Web, `property-web` API/Web và Edge Node.
- Tái hiện một lỗi nghiệp vụ PMS bằng cơ sở dữ liệu PGlite tạm ngoài workspace; dịch vụ thử nghiệm đã được dừng sau khi kiểm tra.
- Rà soát phụ thuộc bằng `npm audit --omit=dev`.

## Kết quả xác minh

- TypeScript đạt: `webadmin/apps/api`, `webadmin/apps/web`, `smart-hotel-os/property-web/apps/api`, `smart-hotel-os/property-web/apps/web`, `smart-hotel-os/apps/edge-node`.
- Bốn dịch vụ trong `smart-hotel-os/services/` chưa có `node_modules` hoặc lockfile tại workspace nên chưa thể typecheck cục bộ.
- Đã tái hiện: `PATCH /api/v1/bookings/:id` đổi booking sang `CHECKED_IN` nhưng phòng vẫn `VACANT` và `power_on=false`.
- `npm audit`: webadmin web có 1 cảnh báo mức cao; property web có 3 cảnh báo mức cao. Property web đang cài `sharp@0.34.5`, không khớp override `^0.35.0`.

## Lỗi/chặn cần người dùng duyệt trước khi sửa

1. Bốn microservice Channel Manager, AI Pricing, IoT và CRM chưa có xác thực/phân quyền; request tự gửi `tenantId`/`propertyId`.
2. Luồng check-in/check-out PMS không bảo toàn trạng thái phòng/điện khi đổi trạng thái qua endpoint PATCH.
3. Edge Node đánh dấu outbox lỗi là `FAILED` nhưng không tự đưa lại hàng đợi; cơ chế nhận dữ liệu Cloud cũng chưa bảo đảm Cloud luôn thắng khi đồng hồ Edge lệch.
4. Mật khẩu SMTP lưu rõ trong `property_settings`, có thể trả qua API cấu hình và ghi vào audit log.
5. Docker Property Web và `smart-hotel-os/services` cùng dùng cổng PostgreSQL 5433.
6. Có cảnh báo bảo mật phụ thuộc `nanoid`, `sharp` và `next`; microservice chưa có dependency lockfile để build tái lập.
7. Booking chưa kiểm tra `checkoutDate` phải sau `checkinDate`.

## Trạng thái Git khi kết thúc phiên

- Nhánh: `main`.
- Commit gốc trước handoff: `fd50944` — `memory.md: cap nhat trang thai Edge Node (da xong, da verify)`.
- Không có thay đổi ứng dụng; thay đổi của phiên này chỉ gồm handoff này và cập nhật quy ước/báo cáo trong `memory.md`.

## Bước kế tiếp

Chờ người dùng duyệt phạm vi sửa cụ thể. Khi được duyệt, ưu tiên: khóa xác thực service và bí mật SMTP, sau đó sửa luồng PMS/Edge Node, cuối cùng là Docker/phụ thuộc/kiểm thử.
