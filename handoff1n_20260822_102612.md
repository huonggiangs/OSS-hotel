# Handoff 2026-08-22 10:26 — Tầng/phòng, tiện ích, hình ảnh và định vị IP

## Phạm vi hoàn thành

- `/basic`: có thể nhập số lượng tầng, thêm/xóa/đổi tên tầng; thêm/xóa phòng trong mỗi tầng với **tên phòng** và **số phòng**. Khi giảm số tầng hoặc xóa tầng có phòng, giao diện yêu cầu xác nhận.
- `/basic`: nút `Lấy vị trí theo IP` đã gọi API Property Web mới `GET /api/v1/location/by-ip`. API dùng `ipwho.is` thay cho `ipapi.co`, vì `ipapi.co` trả HTTP 403 ở mạng hiện tại; có timeout 8 giây, kiểm tra tọa độ và thông báo lỗi rõ ràng.
- `/amenities`: mỗi lần chọn/bỏ chọn tiện ích tự lưu ngay vào DB, hiện trạng hiển thị `Đang lưu...` hoặc `Tự động lưu`.
- `/images`: tạo migration `005_property_images.sql`, repository và API xác thực để thêm/lấy ảnh cơ sở hoặc ảnh theo loại phòng. Giao diện dùng ảnh lưu thực tế, mở hộp chọn PNG/JPG/WebP tối đa 1 MB và xem trước trước khi tải lên.

## Ranh giới dữ liệu

Chi tiết tầng/phòng trên `/basic` là sơ đồ cấu hình cơ sở lưu trong `property_settings.basic`. Không tự tạo bản ghi `rooms` cho vận hành/đặt phòng vì bảng đó bắt buộc loại phòng và khu vực; người dùng chưa yêu cầu/quy định giá trị mặc định cho hai trường này.

## Kiểm thử đã chạy

- `npm run typecheck` tại Property API và Property Web: thành công.
- Docker build/recreate Property API/Web, migration 005: thành công; API/Web/PostgreSQL `healthy`.
- API định vị trong Docker: thành công (khu vực Từ Sơn, Tỉnh Bắc Ninh, Việt Nam); lỗi gốc được xác nhận là `ipapi.co` trả 403, không phải máy mất Internet.
- API ảnh: thêm/lấy một ảnh PNG 1px thành công rồi xóa chính xác dữ liệu kiểm thử; không còn ảnh do phiên này tạo.
- UI: trang `/basic` hiển thị đầy đủ nút thêm/xóa tầng, thêm/xóa phòng và hai trường mỗi phòng; `/amenities` tự lưu, tải lại vẫn giữ lựa chọn; `/images` hiển thị thư viện theo cơ sở/loại phòng và hộp tải ảnh.
- Kiểm tra dữ liệu cuối: 10 tầng, 0 phòng cấu hình; không có dữ liệu kiểm thử còn lại.

## Vận hành

- Truy cập Property Web: `http://localhost:3100`.
- Docker tiếp tục chạy nền qua Docker Desktop với `restart: unless-stopped`; có thể đóng Codex.
- Tài khoản demo quản lý: `manager` / `Anio2026@`.

## Bước tiếp theo cần duyệt nếu muốn mở rộng

- Nếu cần các phòng nhập ở `/basic` xuất hiện ngay trong trạng thái phòng/đặt phòng, cần duyệt quy tắc tạo phòng vận hành: loại phòng mặc định, khu vực/tòa nhà và trạng thái khởi tạo.
- Nếu cần xóa ảnh đã tải, cần duyệt bổ sung nút/API xóa ảnh.
