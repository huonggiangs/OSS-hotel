# Handoff 2026-08-22 17:10 — 9 màn hình Cài đặt Property Web: CRUD thật, SePay, QR, xuất dữ liệu

## Phạm vi hoàn thành

Đúng theo yêu cầu, đã rà soát code thật (không suy diễn) rồi sửa cho 9 trang:

- **`/price`**: Sửa/Xóa hoạt động thật cho "Danh sách loại phòng" và "Danh sách phòng";
  form Thêm loại phòng/Thêm phòng nhập được và lưu thật; công tắc "Sync" theo từng phòng là
  cờ thật trong DB (đánh dấu sẵn sàng đồng bộ, chưa gọi API OTA thật); "QR Code" mở ảnh QR
  trỏ tới trang công khai `/guest/room/[token]` để khách xem thông tin phòng + quét thanh
  toán trước (nếu SePay đã bật).
- **`/payment`**: các kênh chưa có API thật (VNPay/MoMo/ZaloPay/Stripe...) vẫn hiển thị
  nhưng bị khoá thao tác rõ ràng; thêm **SePay** làm cổng thanh toán thật (API Token mã
  hoá, webhook nhận giao dịch, nút đồng bộ thủ công, nhúng QR VietQR động).
- **`/currency`**: thêm/sửa/xoá tiền tệ, checkbox lấy tỷ giá tự động (code đúng, xem giới
  hạn mạng sandbox bên dưới).
- **`/tax`**: thêm/sửa/xoá thuế/phí qua popup thật.
- **`/time`**: toàn bộ trường sửa được (giờ nhận/trả, định dạng, múi giờ, làm tròn, ngày
  lễ...); sửa luôn lỗi ô chọn ngày trước đây không gán được vào đúng trường.
- **`/printer`**: nhập tên máy in + in thử thật qua hộp thoại in hệ điều hành (trình duyệt
  không thể tự phát hiện danh sách máy in — giới hạn nền tảng, đã giải thích trong UI);
  CRUD mẫu in thật.
- **`/sync`**: danh sách kết nối OTA CRUD thật theo từng nhà cung cấp (mã cơ sở, API key mã
  hoá, cờ đồng bộ) — vẫn là CẤU HÌNH, chưa đồng bộ thật với Booking/Agoda/Airbnb (cần hợp
  đồng đối tác thật, đã ghi rõ trên trang).
- **`/db`**: nút "Sao lưu ngay"/"Xuất dữ liệu" tải về thật 1 file JSON toàn bộ dữ liệu cơ
  sở (không có mật khẩu).
- **`/users`**: sửa/thêm vai trò lưu thật — có cảnh báo rõ khi tên vai trò không khớp 1
  trong 4 vai trò có phân quyền thật ở API.
- **`/assets`**: thêm/sửa/xoá tài sản thật, xuất file CSV phục vụ kiểm kê.

## Quyết định phạm vi quan trọng (ghi ADR đầy đủ tại `smart-hotel-os/DECISIONS.md`)

- **ADR-009**: chỉ SePay là cổng thanh toán thật; các cổng khác giữ nguyên hiển thị nhưng
  khoá thao tác thay vì giả vờ hoạt động.
- **ADR-010**: "đồng bộ hoá" OTA chỉ là cấu hình kết nối, chưa gọi API thật của bất kỳ OTA
  nào (chưa có hợp đồng/credential đối tác).
- **ADR-011**: "vai trò" ở Property Web là bảng mô tả; phân quyền thật ở API vẫn chỉ có 4
  giá trị cố định.

## Kiểm thử đã chạy

- `npx tsc --noEmit` sạch cho `apps/api` và `apps/web`.
- `next build` thành công đủ 32 route (kể cả `/guest/room/[token]` mới).
- Chạy DB nhúng từ đầu: migration 001→007 áp dụng tuần tự không lỗi.
- Curl end-to-end thật cho toàn bộ API mới: CRUD loại phòng/phòng (kể cả case xoá bị chặn
  409 khi còn phòng gắn vào), công tắc sync, ảnh QR PNG hợp lệ (đã mở kiểm tra header PNG),
  trang công khai theo token (200 đúng token / 404 token sai), SePay (token không lộ dạng
  rõ trong response, webhook khớp đúng hoá đơn + chống xử lý trùng theo id giao dịch), xuất
  dữ liệu (401 khi chưa đăng nhập, xác nhận 0 lần xuất hiện `password_hash`), sửa vai trò,
  đường lỗi tỷ giá tự động khi mạng sandbox chặn.

## Giới hạn đã nêu rõ, không che giấu

- Tỷ giá ngoại tệ tự động: code + endpoint đúng, nhưng mạng của môi trường thử nghiệm hiện
  tại chặn mọi API tỷ giá miễn phí đã thử (allowlist) — chưa xác nhận được đường thành công
  thật, chỉ xác nhận đường lỗi hoạt động đúng. Sẽ hoạt động khi chạy ở môi trường có
  Internet đầy đủ (Docker Desktop trên máy bạn).
- Webhook SePay cần URL công khai (không phải `localhost`) mới nhận được real-time — chạy
  cục bộ dùng nút "Đồng bộ giao dịch ngay" thay thế.
- Máy in: không có cách nào để trình duyệt tự liệt kê máy in đã cài trên máy tính — đây là
  giới hạn của mọi ứng dụng web, không phải thiếu sót riêng của hệ thống này.

## Vận hành

- Chạy như cũ: `powershell -ExecutionPolicy Bypass -File "D:\hotel\OSS\start-all.ps1"` hoặc
  Docker (`ops\scripts\Start-Oss.ps1`) nếu đang dùng.
- Tài khoản demo: `manager` / `Anio2026@` (Property Web).
- **Cần bạn làm nếu muốn dùng SePay thật**: tạo API Token tại SePay (Cấu hình Công ty → API
  Access), điền vào `/payment`, và dán URL webhook (hiển thị sẵn trên trang, có nút sao
  chép) vào mục Webhooks trong dashboard SePay — chỉ hoạt động khi Property Web có địa chỉ
  công khai (không phải `localhost`).

## Bước tiếp theo cần duyệt nếu muốn mở rộng

- Đồng bộ OTA thật (Booking/Agoda/Airbnb) — cần hợp đồng đối tác + credential thật trước.
- RBAC động thật cho vai trò mới — cần sửa tầng enum DB + toàn bộ `requireRole` trong API,
  là thay đổi mô hình bảo mật, cần duyệt riêng.
- Xác nhận tỷ giá tự động chạy thành công thật ở môi trường có Internet đầy đủ.
