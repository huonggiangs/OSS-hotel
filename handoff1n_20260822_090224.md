# Handoff 2026-08-22 09:02 — Cấu hình Cơ bản lưu DB

## Phạm vi hoàn thành

Đã cập nhật `http://localhost:3100/basic` theo yêu cầu và lưu qua API Property Web vào PostgreSQL (`property_settings`, group `basic`):

- Chọn, xem trước, xóa và lưu logo cơ sở.
- Nhập số lượng tầng hoặc thêm một tầng; danh sách tầng được tạo/lưu cùng cấu hình.
- Chọn phân loại cơ sở lưu trú: Khách sạn, Nhà nghỉ, Homestay, Resort, Villa/Biệt thự du lịch, Căn hộ du lịch, Hostel, Khác.
- Lấy vị trí gần đúng theo IP khi người dùng bấm nút, sau đó hiển thị Google Maps và lưu địa chỉ khu vực + latitude/longitude.
- Nhập/lưu thông tin chủ sở hữu và thông tin tài khoản thanh toán.
- Nút `Cập nhật` là button thật, có trạng thái đang lưu và xác nhận đã lưu DB.

## An toàn dữ liệu

- Logo chỉ nhận PNG/JPG/WebP và tối đa 750 KB. Ảnh được lưu data URL trong DB để không phụ thuộc file cục bộ; request API giới hạn 2 MB và schema backend kiểm tra hình dạng/dung lượng.
- Dữ liệu ảnh không được chép vào audit log; audit chỉ ghi `[IMAGE_DATA_OMITTED]`.
- Nút `Lấy vị trí theo IP` không tự chạy lúc mở trang. Lần bấm sẽ gửi IP công khai đến `ipapi.co` để lấy kết quả gần đúng cấp thành phố/khu vực, sau đó Google Maps hiển thị bằng tọa độ. Không thay thế bằng định vị GPS hoặc địa chỉ chính xác.

## Kiểm thử đã chạy

- `npm run typecheck`: Property API và Property Web thành công.
- Docker production build: Property API và Property Web thành công.
- UI: chọn phân loại, nhập/thêm tầng, lưu và tải lại; các tab chủ sở hữu/thanh toán lưu thành công.
- API: logo PNG data URL, phân loại, tọa độ IP, floor list lưu rồi GET lại đúng; dữ liệu kiểm thử đã hoàn tác về giá trị rỗng/mặc định.
- `Get-OssStatus.ps1`: toàn bộ stack OSS `healthy`, các health API HTTP 200.

## Lưu ý bàn giao

- Không bấm nút định vị IP trong kiểm thử để không truyền IP công khai của máy đến dịch vụ bên thứ ba. Chức năng đã sẵn sàng; người dùng cần tự bấm nút trên trang khi đồng ý với việc định vị IP.
- Mục vị trí theo IP có độ chính xác giới hạn; nếu cần địa chỉ khách sạn chính xác/chọn pin trên bản đồ, cần người dùng duyệt bổ sung luồng nhập địa chỉ và Google Maps Geocoding API.
