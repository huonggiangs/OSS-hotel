# Bắt buộc cho mọi API microservice. Sinh một giá trị riêng bằng:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SERVICE_API_KEY=replace-with-a-unique-base64-key-of-at-least-32-characters
