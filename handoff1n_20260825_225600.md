# Handoff — 2026-08-25: Deploy, LAN và auto-update Docker

## Kết quả

- Backup trước thay đổi: `backups/oss_20260825_224117/`; backup script đã chạy
  và xác minh: `backups/oss_20260825_225252_verify-automation/`.
- Rebuild Docker toàn bộ qua `ops/scripts/Start-Oss.ps1`; 9 service healthy.
  Property PostgreSQL đã có migration `001`–`007` (trước đây runtime chỉ có 001–005).
- Xác minh LAN hiện tại bằng script: HQ `http://192.168.1.14:3000`, PMS
  `http://192.168.1.14:3100`, Edge `http://192.168.1.14:4200`, đều HTTP 200.
  IP DHCP có thể đổi: luôn chạy `ops/scripts/Test-OssLan.ps1`, không dùng IP này
  như một cấu hình cố định.
- HQ/PMS frontend đã chuyển sang same-origin proxy `/api/backend`; login HQ và
  PMS qua LAN proxy thành công, Property data export HTTP 200 có token.
- Foreign key `devices.room_id` được xử lý trước insert bằng check ownership
  atomic. `roomId` giả trả HTTP 404, `orphan_devices=0`, hai FK `devices` đều
  validated. Một error SQL trong log 22:54 là do lệnh kiểm tra chẩn đoán gõ sai
  dấu quote, không phải request ứng dụng; không có foreign-key runtime mới.
- Watcher đã chạy và được smoke-test: thay comment source → typecheck → rebuild
  `oss-property` → healthcheck pass. PID sẽ thay đổi sau restart; dùng
  `Get-OssStatus.ps1` thay vì ghi nhớ PID.

## Quy ước vận hành mới

- Docker/LAN canonical: web `3000/3100/4200`; Docker API `4000/4100` loopback.
- Dev không Docker: HQ `13000/14000`, PMS `13100/14100`, Edge `14200`.
- `Watch-Oss.ps1` tự deploy source hợp lệ sau debounce; migration SQL sẽ backup
  trước deploy. Không sửa migration đã áp dụng.
- Windows Startup đã đổi từ chỉ mở Docker Desktop sang gọi `Start-Oss.ps1`, nên
  sau đăng nhập Docker, healthcheck và watcher được khôi phục.
- Agent instructions: `AGENTS.md`, `CLAUDE.md`, `.cursorrules`.

## Bước tiếp theo

1. Commit và push thay đổi phiên này (bao gồm commit cũ `1feaa31` đang ahead).
2. Người dùng thử đăng nhập từ một máy/điện thoại khác cùng Wi-Fi bằng URL do
   `Test-OssLan.ps1` in ra.
3. Trước public Internet, thực hiện checklist `ops/PUBLIC_DEPLOYMENT_CHECKLIST.md`.
   Môi trường một replica có downtime ngắn trong lúc thay image; cần blue-green/
   rolling deployment sau reverse proxy để đạt zero-downtime thật.
